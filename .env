# Django
DJANGO_SECRET_KEY=change-me-in-production
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Database: "sqlite" for zero-setup local development, "postgresql" for
# staging/production (required — matches the shared-database architecture).
DATABASE_ENGINE=sqlite

# Used only when DATABASE_ENGINE=sqlite
SQLITE_NAME=db.sqlite3

# Used only when DATABASE_ENGINE=postgresql
POSTGRES_DB=a2z_icsem
POSTGRES_USER=a2z_icsem
POSTGRES_PASSWORD=change-me
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# JWT
JWT_ACCESS_TOKEN_LIFETIME_MINUTES=30
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7
JWT_SIGNING_KEY=

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Field-level encryption (restricted PII / credential secure_details)
FIELD_ENCRYPTION_KEY=k7KlY_LNmllOqOA8JrGDu8rnkTbsLLcizirF8JHUIGk=

# Email (OTP delivery, notifications). Leave EMAIL_BACKEND unset to use the
# console backend while DJANGO_DEBUG=True (prints to the runserver output,
# zero setup). Set it to django.core.mail.backends.smtp.EmailBackend plus
# the fields below for real delivery. EMAIL_HOST_PASSWORD is a Gmail App
# Password, not the account password, if using Gmail SMTP.
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.gmail.com'
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = 'iiiqbetspython@gmail.com'
EMAIL_HOST_PASSWORD = 'xtcu kzku lvzd rpof'


